#include "main.h"

/**
 * _isuper - checks for uppercase character
 * @c: the character is to be checked
 * Return: 1 if c is uppercase, 0 othrewise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
