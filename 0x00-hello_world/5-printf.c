#include <stdio.h>
/**
 * main - prints the word with proper grammar,
 * but the outcome is a piece of art,
 * Return - zero if exited properly, non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
